import React from "react";
// import "./input.scss";

const Name = ({ placeHolder }) => {
  return (
    <>
      <input type="text" placeholder={placeHolder} />
    </>
  );
};

export default Name;
